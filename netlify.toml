[build]
  publish = "."
  functions = "netlify/functions"

# Le funzioni sono raggiungibili come /api/<nome>
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[functions]
  node_bundler = "esbuild"
